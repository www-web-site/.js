const askName = prompt('Ismingizni kiriting!');

const askAge = prompt('Yoshingizni kiriting!');

const askRegion = prompt('Manzilingizni kiriting!');

let notify ='Salom mening ismim ' + askName + ' va men ' + askAge + ' yosh daman. Men ' + askRegion + ' danman ';

alert (notify);